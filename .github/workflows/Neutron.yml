name: Build Kernel with Slim LLVM

on:
  workflow_dispatch:
    inputs:
      kernel_branch:
        description: 'Branch to build (e.g., LosKernel-23)'
        required: true
        default: 'Staging-Kernel'
      defconfig:
        description: 'Defconfig name (without path)'
        required: true
        default: 'vendor/lahaina-qgki_defconfig'
      enable_kernelsu_next:
        description: 'Enable KernelSU Next (yes/no)'
        required: false
        default: 'no'
      enable_nomount:
        description: 'Integrate maxsteeel/nomount (dev branch, yes/no)'
        required: false
        default: 'no'
      enable_eva:
        description: 'Integrate E.V.A (yes/no)'
        required: false
        default: 'no'
      enable_basebandguard:
        description: 'Integrate Baseband-guard LSM (yes/no)'
        required: false
        default: 'no'
      package_modules:
        description: 'Pack built .ko modules into the AnyKernel3 zip (yes/no)'
        required: false
        default: 'yes'
      build_tag:
        description: 'Build tag for ZIP name (e.g., Test/Perf/Normal)'
        required: false
        default: 'Normal'
      enable_signing:
        description: 'Sign the flashable ZIP with Tenzo keystore (yes/no)'
        required: false
        default: 'no'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout kernel source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.kernel_branch }}

      - name: Notify Telegram - build start
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d parse_mode="html" \
            -d disable_web_page_preview=true \
            -d text="🔨 <b>Kernel Build Triggered</b>%0A<b>Branch:</b> <code>${{ github.event.inputs.kernel_branch }}</code>%0A<b>Defconfig:</b> <code>${{ github.event.inputs.defconfig }}</code>%0A<b>KernelSU Next:</b> <code>${{ github.event.inputs.enable_kernelsu_next }}</code>%0A<b>Signing:</b> <code>${{ github.event.inputs.enable_signing }}</code>%0A<b>Tag:</b> <code>${{ github.event.inputs.build_tag }}</code>%0A<b>Triggered by:</b> <code>${{ github.actor }}</code>"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bc bison flex libssl-dev make libelf-dev lz4 cpio \
            gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi \
            build-essential device-tree-compiler \
            python3 python3-pip zip git ccache wget xz-utils zstd aria2 \
            default-jre openjdk-21-jdk

      - name: Install mkdtboimg
        run: |
          set -e
          MKDTBOIMG="/usr/local/bin/mkdtboimg.py"
          if [ -f "scripts/mkdtboimg.py" ]; then
            echo "✅ Found mkdtboimg.py in kernel tree (scripts/mkdtboimg.py) — using it"
            cp scripts/mkdtboimg.py "$MKDTBOIMG"
            chmod +x "$MKDTBOIMG"
          else
            echo "⚠️  scripts/mkdtboimg.py not found in kernel tree"
            echo "📥 Downloading mkdtboimg.py from AOSP googlesource..."
            wget -q \
              "https://android.googlesource.com/platform/system/libufdt/+/refs/heads/master/utils/src/mkdtboimg.py?format=TEXT" \
              -O mkdtboimg_b64.txt
            base64 -d mkdtboimg_b64.txt > "$MKDTBOIMG"
            chmod +x "$MKDTBOIMG"
            rm mkdtboimg_b64.txt
          fi
          python3 "$MKDTBOIMG" --help > /dev/null 2>&1 \
            && echo "✅ mkdtboimg.py verified successfully" \
            || { echo "❌ mkdtboimg.py verification failed"; exit 1; }
          echo "MKDTBOIMG=$MKDTBOIMG" >> $GITHUB_ENV

      # -------------------------------------------------------------------
      # Fetch Slim LLVM 23.1.0, extract it, export all required build
      # environment variables — mirroring Neutron Clang's structure exactly
      # so the make invocations below are identical between both workflows.
      # -------------------------------------------------------------------
      - name: Setup Slim LLVM toolchain and export build environment
        run: |
          set -e
          echo "📥 Downloading Slim LLVM 23.1.0..."
          mkdir -p "$HOME/build_toolchain"
          cd "$HOME/build_toolchain"
          aria2c -q https://mirrors.edge.kernel.org/pub/tools/llvm/files/llvm-23.1.0-x86_64.tar.xz
          tar -xJf llvm-23.1.0-x86_64.tar.xz
          cd "$GITHUB_WORKSPACE"

          TOOLCHAIN_DIR="$HOME/build_toolchain/llvm-23.1.0-x86_64"
          CLANG_PATH="$TOOLCHAIN_DIR/bin"

          if [ ! -x "$CLANG_PATH/clang" ]; then
            echo "❌ clang binary not found in $CLANG_PATH"
            exit 1
          fi

          CLANG_VERSION=$("$CLANG_PATH/clang" --version | head -n1 | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
          COMPILER_STRING="Slim LLVM clang version ${CLANG_VERSION} (https://mirrors.edge.kernel.org/pub/tools/llvm/), Slim LLD ${CLANG_VERSION}"
          echo "✅ Slim LLVM installed: $CLANG_VERSION"

          # Prepend toolchain bin to PATH for all later steps
          echo "$CLANG_PATH" >> $GITHUB_PATH

          # Export all build env vars — each written individually via
          # echo to avoid any heredoc YAML parsing issues
          echo "TOOLCHAIN_DIR=$TOOLCHAIN_DIR"             >> $GITHUB_ENV
          echo "CLANG_LABEL=Slim LLVM ${CLANG_VERSION}"  >> $GITHUB_ENV
          echo "KBUILD_COMPILER_STRING=$COMPILER_STRING"  >> $GITHUB_ENV
          echo "ARCH=arm64"                               >> $GITHUB_ENV
          echo "SUBARCH=arm64"                            >> $GITHUB_ENV
          echo "LLVM=1"                                   >> $GITHUB_ENV
          echo "LLVM_IAS=1"                               >> $GITHUB_ENV
          echo "CC=$CLANG_PATH/clang"                     >> $GITHUB_ENV
          echo "AR=$CLANG_PATH/llvm-ar"                   >> $GITHUB_ENV
          echo "NM=$CLANG_PATH/llvm-nm"                   >> $GITHUB_ENV
          echo "LD=$CLANG_PATH/ld.lld"                    >> $GITHUB_ENV
          echo "OBJCOPY=$CLANG_PATH/llvm-objcopy"         >> $GITHUB_ENV
          echo "OBJDUMP=$CLANG_PATH/llvm-objdump"         >> $GITHUB_ENV
          echo "STRIP=$CLANG_PATH/llvm-strip"             >> $GITHUB_ENV
          echo "CLANG_TRIPLE=aarch64-linux-gnu-"          >> $GITHUB_ENV
          echo "CROSS_COMPILE=aarch64-linux-gnu-"         >> $GITHUB_ENV
          echo "CROSS_COMPILE_ARM32=arm-linux-gnueabi-"   >> $GITHUB_ENV
          echo "KBUILD_BUILD_USER=willay"                  >> $GITHUB_ENV
          echo "KBUILD_BUILD_HOST=GNU"                    >> $GITHUB_ENV
          echo "KBUILD_BUILD_VERSION=1"                   >> $GITHUB_ENV
          echo "✅ All build environment variables exported for Slim LLVM"

      - name: Setup KernelSU (if enabled)
        if: ${{ github.event.inputs.enable_kernelsu_next == 'yes' }}
        run: |
          echo "🔧 Enabling KernelSU..."
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -
          rm -rf KernelSU
          git clone -b master-susfs https://github.com/William24hmar/KernelSU.git KernelSU
          DEFCONFIG_PATH="arch/arm64/configs/${{ github.event.inputs.defconfig }}"
          if [ -f "$DEFCONFIG_PATH" ]; then
            echo "✅ Adding CONFIG_KSU=y to defconfig"
            echo -e "\n# KernelSU\nCONFIG_KSU=y" >> "$DEFCONFIG_PATH"
          else
            echo "⚠️  Defconfig not found at $DEFCONFIG_PATH — skipping CONFIG_KSU injection"
          fi

      - name: Setup nomount (if enabled)
        if: ${{ github.event.inputs.enable_nomount == 'yes' }}
        run: |
          echo "🔧 Enabling NoMount..."
          curl https://raw.githubusercontent.com/maxsteeel/nomount/refs/heads/dev/kernel/setup.sh | bash -s dev
          rm -rf NoMount
          git clone -b dev https://github.com/Willay24/nomount.git NoMount
          echo "✅ nomount (dev) setup complete"

      - name: Setup E.V.A (if enabled)
        if: ${{ github.event.inputs.enable_eva == 'yes' }}
        run: |
          echo "🔧 Enabling E.V.A..."
          curl -LSs "https://raw.githubusercontent.com/Willay24/E.V.A/main/kernel/setup.sh" | bash -s main
          echo "✅ E.V.A setup complete"

      - name: Setup Baseband-guard (if enabled)
        if: ${{ github.event.inputs.enable_basebandguard == 'yes' }}
        run: |
          set -e
          DEFCONFIG_PATH="arch/arm64/configs/${{ github.event.inputs.defconfig }}"
          echo "🛡️  Integrating Baseband-guard LSM..."
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash
          if [ -f "$DEFCONFIG_PATH" ]; then
            echo "✅ Injecting CONFIG_BBG=y into defconfig ($DEFCONFIG_PATH)"
            echo -e "\n# Baseband-guard\nCONFIG_BBG=y" >> "$DEFCONFIG_PATH"
          else
            echo "⚠️  Defconfig not found at $DEFCONFIG_PATH — skipping CONFIG_BBG injection"
          fi
          if [ -f "security/Kconfig" ]; then
            echo "🩹 Patching security/Kconfig LSM default list..."
            sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' security/Kconfig
            echo "✅ security/Kconfig patched (baseband_guard added to LSM default list)"
          else
            echo "⚠️  security/Kconfig not found — skipping LSM default patch"
          fi

      # -------------------------------------------------------------------
      # Configure + build. MAKE_ARGS mirrors the Neutron Clang workflow
      # exactly — all vars are now properly set from the toolchain step.
      # -------------------------------------------------------------------
      - name: Configure kernel (defconfig)
        run: |
          set -e
          export KCFLAGS="-Wno-unknown-warning-option"
          make O=out \
            ARCH=$ARCH \
            SUBARCH=$SUBARCH \
            CC=$CC \
            AR=$AR \
            NM=$NM \
            LD=$LD \
            OBJCOPY=$OBJCOPY \
            OBJDUMP=$OBJDUMP \
            STRIP=$STRIP \
            CROSS_COMPILE=$CROSS_COMPILE \
            CROSS_COMPILE_COMPAT=$CROSS_COMPILE_ARM32 \
            CLANG_TRIPLE=$CLANG_TRIPLE \
            HOSTCC=$CC \
            HOSTCXX=$TOOLCHAIN_DIR/bin/clang++ \
            LLVM=$LLVM \
            LLVM_IAS=$LLVM_IAS \
            KBUILD_BUILD_USER=$KBUILD_BUILD_USER \
            KBUILD_BUILD_HOST=$KBUILD_BUILD_HOST \
            KCFLAGS="$KCFLAGS" \
            ${{ github.event.inputs.defconfig }}

      - name: Build kernel Image and dtbs
        run: |
          set -e
          export KCFLAGS="-Wno-unknown-warning-option"
          set -o pipefail
          make O=out \
            ARCH=$ARCH \
            SUBARCH=$SUBARCH \
            CC=$CC \
            AR=$AR \
            NM=$NM \
            LD=$LD \
            OBJCOPY=$OBJCOPY \
            OBJDUMP=$OBJDUMP \
            STRIP=$STRIP \
            CROSS_COMPILE=$CROSS_COMPILE \
            CROSS_COMPILE_COMPAT=$CROSS_COMPILE_ARM32 \
            CLANG_TRIPLE=$CLANG_TRIPLE \
            HOSTCC=$CC \
            HOSTCXX=$TOOLCHAIN_DIR/bin/clang++ \
            LLVM=$LLVM \
            LLVM_IAS=$LLVM_IAS \
            KBUILD_BUILD_USER=$KBUILD_BUILD_USER \
            KBUILD_BUILD_HOST=$KBUILD_BUILD_HOST \
            KCFLAGS="$KCFLAGS" \
            -j$(nproc --all) Image dtbs 2>&1 | tee build.log
          echo "✅ Kernel build completed"

      - name: Verify Image was produced
        run: |
          if [ -f "out/arch/arm64/boot/Image" ]; then
            echo "✅ Image found — proceeding to dtb/dtbo packaging"
          else
            echo "❌ Image not found — kernel build truly failed"
            exit 1
          fi

      - name: Build dtb / dtb.img / dtbo.img
        run: |
          set -e
          BOOT_OUT="out/arch/arm64/boot"
          DTS_OUT="$BOOT_OUT/dts"
          echo "🔗 Merging .dtb files into dtb blob..."
          if find "$DTS_OUT" -name '*.dtb' 2>/dev/null | grep -q .; then
            find "$DTS_OUT" -name '*.dtb' -exec cat {} + > "$BOOT_OUT/dtb"
            echo "✅ dtb blob created ($(wc -c < "$BOOT_OUT/dtb") bytes)"
          else
            echo "❌ No .dtb files found — cannot create dtb blob"
            exit 1
          fi
          echo "📦 Creating dtb.img..."
          mapfile -t DTB_LIST < <(find "$DTS_OUT" -name '*.dtb' | sort)
          python3 "$MKDTBOIMG" create "$BOOT_OUT/dtb.img" "${DTB_LIST[@]}" \
            && echo "✅ dtb.img created (${#DTB_LIST[@]} dtb entries)" \
            || { echo "❌ Failed to create dtb.img"; exit 1; }
          if [ -f "$BOOT_OUT/dtbo.img" ]; then
            echo "✅ dtbo.img already present (built by kernel)"
          elif find "$DTS_OUT" -name '*.dtbo' 2>/dev/null | grep -q .; then
            echo "📦 Creating dtbo.img from .dtbo overlay files..."
            mapfile -t DTBO_LIST < <(find "$DTS_OUT" -name '*.dtbo' | sort)
            python3 "$MKDTBOIMG" create "$BOOT_OUT/dtbo.img" "${DTBO_LIST[@]}" \
              && echo "✅ dtbo.img created (${#DTBO_LIST[@]} dtbo entries)" \
              || echo "⚠️  Failed to create dtbo.img — continuing anyway"
          else
            echo "⚠️  No .dtbo files and no pre-built dtbo.img found"
          fi
          echo ""
          echo "── Boot artifacts ──────────────────────"
          ls -lh "$BOOT_OUT/Image" "$BOOT_OUT/dtb" "$BOOT_OUT/dtb.img" "$BOOT_OUT/dtbo.img" 2>/dev/null || true
          echo "────────────────────────────────────────"

      - name: Build and install kernel modules
        if: ${{ github.event.inputs.package_modules == 'yes' }}
        run: |
          set -e
          export KBUILD_BUILD_USER="willay"
          export KBUILD_BUILD_HOST="GNU"
          MAKE_ARGS="O=out ARCH=arm64 LLVM=1 LLVM_IAS=1"
          echo "🔨 Building kernel modules..."
          make -j$(nproc --all) $MAKE_ARGS modules
          if find out -name '*.ko' 2>/dev/null | grep -q .; then
            echo "📦 Installing modules to out/modules..."
            make -j$(nproc --all) $MAKE_ARGS \
              INSTALL_MOD_PATH=modules INSTALL_MOD_STRIP=1 modules_install
            echo "✅ Modules built and staged under out/modules"
          else
            echo "⚠️  No .ko files were produced — skipping modules_install"
          fi

      - name: Prepare AnyKernel3 package
        run: |
          git clone --depth=1 https://github.com/William24hmar/AnyKernel3.git -b master anykernel
          BOOT_OUT="out/arch/arm64/boot"
          cp "$BOOT_OUT/Image" anykernel/
          echo "✅ Image copied"
          [ -f "$BOOT_OUT/dtb" ]      && cp "$BOOT_OUT/dtb"      anykernel/ && echo "✅ dtb copied"      || echo "⚠️  dtb not found"
          [ -f "$BOOT_OUT/dtb.img" ]  && cp "$BOOT_OUT/dtb.img"  anykernel/ && echo "✅ dtb.img copied"  || echo "⚠️  dtb.img not found"
          [ -f "$BOOT_OUT/dtbo.img" ] && cp "$BOOT_OUT/dtbo.img" anykernel/ && echo "✅ dtbo.img copied" || echo "⚠️  dtbo.img not found"

      - name: Package kernel modules for AnyKernel3
        if: ${{ github.event.inputs.package_modules == 'yes' }}
        run: |
          set -e
          if [ ! -d out/modules ]; then
            echo "⚠️  out/modules not present — skipping"
            exit 0
          fi
          KERNEL_RELEASE=$(cat out/include/config/kernel.release 2>/dev/null || true)
          if [ -z "$KERNEL_RELEASE" ]; then
            KERNEL_RELEASE=$(find out/modules/lib/modules -maxdepth 1 -mindepth 1 -type d -printf '%f\n' | head -n1)
          fi
          if [ -z "$KERNEL_RELEASE" ]; then
            echo "❌ Could not determine kernel release string — aborting module packaging"
            exit 1
          fi
          echo "📋 Kernel release: $KERNEL_RELEASE"
          MOD_DEST="anykernel/modules/vendor/lib/modules"
          mkdir -p "$MOD_DEST"
          KO_FILES=()
          while IFS= read -r -d '' f; do
            KO_FILES+=("$f")
          done < <(find out -path out/modules -prune -o -name '*.ko' -print0 2>/dev/null)
          while IFS= read -r -d '' f; do
            base=$(basename "$f")
            already=0
            for existing in "${KO_FILES[@]:-}"; do
              [ "$(basename "$existing")" = "$base" ] && already=1 && break
            done
            [ "$already" -eq 0 ] && KO_FILES+=("$f")
          done < <(find . -maxdepth 6 -name 'nomount.ko' -not -path './out/*' -print0 2>/dev/null)
          if [ ${#KO_FILES[@]} -eq 0 ]; then
            echo "⚠️  No .ko files found — skipping"
            exit 0
          fi
          declare -A seen=()
          copied=0; skipped=0
          for f in "${KO_FILES[@]}"; do
            base=$(basename "$f")
            if [ -n "${seen[$base]:-}" ]; then
              echo "   ↷ skipping duplicate $base"
              skipped=$((skipped + 1))
              continue
            fi
            cp "$f" "$MOD_DEST/$base"
            seen[$base]="$f"
            copied=$((copied + 1))
          done
          echo "📦 Copied $copied .ko file(s) ($skipped duplicate(s) skipped)"
          MODSRC_DIR=$(find out/modules/lib/modules -maxdepth 1 -mindepth 1 -type d | head -n1)
          if [ -n "$MODSRC_DIR" ]; then
            for f in modules.alias modules.dep modules.softdep; do
              [ -f "$MODSRC_DIR/$f" ] && cp "$MODSRC_DIR/$f" "$MOD_DEST/" && echo "✅ $f copied"
            done
            [ -f "$MODSRC_DIR/modules.order" ] && cp "$MODSRC_DIR/modules.order" "$MOD_DEST/modules.load" && echo "✅ modules.load copied"
            for f in "$MODSRC_DIR"/modules.*; do
              base=$(basename "$f"); [ -f "$MOD_DEST/$base" ] || cp "$f" "$MOD_DEST/"
            done
          fi
          [ -f "$MOD_DEST/modules.dep" ] && \
            sed -i 's/\(kernel\/[^: ]*\/\)\([^: ]*\.ko\)/\/vendor\/lib\/modules\/\2/g' "$MOD_DEST/modules.dep" && \
            echo "✅ modules.dep paths rewritten"
          [ -f "$MOD_DEST/modules.load" ] && \
            sed -i 's/.*\///g' "$MOD_DEST/modules.load" && \
            echo "✅ modules.load reduced to bare filenames"
          echo ""
          echo "── Packaged modules ────────────────────"
          ls -lh "$MOD_DEST" | grep -E '\.ko$|modules\.' || true
          echo "────────────────────────────────────────"

      - name: Create flashable ZIP
        run: |
          RAW_TAG="${{ github.event.inputs.build_tag }}"
          SAFE_TAG="${RAW_TAG// /-}"
          SAFE_TAG="${SAFE_TAG//\//-}"
          [ "${{ github.event.inputs.enable_basebandguard }}" = "yes" ] && SAFE_TAG="${SAFE_TAG}-BBG"
          [ "${{ github.event.inputs.enable_nomount }}"      = "yes" ] && SAFE_TAG="${SAFE_TAG}-nomount"
          [ "${{ github.event.inputs.enable_eva }}"          = "yes" ] && SAFE_TAG="${SAFE_TAG}-EVA"
          cd anykernel
          ZIP_NAME="SlimLLVM-${SAFE_TAG}-$(date +%Y%m%d-%H%M).zip"
          zip -r9 "../$ZIP_NAME" . -x "*.git*" -x "README.md"
          cd ..
          echo "ZIP_FILE=$ZIP_NAME" >> $GITHUB_ENV
          echo "✅ Zip created: $ZIP_NAME ($(du -sh $ZIP_NAME | cut -f1))"

      - name: Generate signing keystore
        if: ${{ github.event.inputs.enable_signing == 'yes' }}
        run: |
          set -e
          SIGNER_DIR="$HOME/.gf"
          KEYSTORE="$SIGNER_DIR/keystore.p12"
          if [ -f "$KEYSTORE" ]; then echo "✔ Keystore already exists"; exit 0; fi
          rm -rf "$SIGNER_DIR"; mkdir -p "$SIGNER_DIR" && chmod 700 "$SIGNER_DIR"
          PASS=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 18)
          UNIQUE_ID=$(date +%Y%m%d_%H%M%S)
          KEYALIAS="tenzoKey_${UNIQUE_ID}"
          echo "$PASS" > "$SIGNER_DIR/.store_pass"
          echo "$KEYALIAS" > "$SIGNER_DIR/.alias"
          chmod 600 "$SIGNER_DIR/.store_pass" "$SIGNER_DIR/.alias"
          openssl genrsa -out "$SIGNER_DIR/rootCA.key" 4096
          openssl req -x509 -new -nodes -key "$SIGNER_DIR/rootCA.key" -sha256 -days 9125 \
            -out "$SIGNER_DIR/rootCA.pem" -subj "/CN=Tenzo Root CA/O=Madara273/C=UA"
          openssl genrsa -out "$SIGNER_DIR/tenzo.key" 4096
          openssl req -new -key "$SIGNER_DIR/tenzo.key" -out "$SIGNER_DIR/tenzo.csr" \
            -subj "/CN=Tenzo, OU=Root, O=Madara273, L=UA, ST=Ukraine, C=UA"
          openssl x509 -req -in "$SIGNER_DIR/tenzo.csr" \
            -CA "$SIGNER_DIR/rootCA.pem" -CAkey "$SIGNER_DIR/rootCA.key" \
            -CAcreateserial -out "$SIGNER_DIR/tenzo.crt" -sha256 -days 365
          openssl pkcs12 -export \
            -inkey "$SIGNER_DIR/tenzo.key" -in "$SIGNER_DIR/tenzo.crt" \
            -certfile "$SIGNER_DIR/rootCA.pem" -out "$SIGNER_DIR/tenzo.p12" \
            -password pass:"$PASS" -name "$KEYALIAS"
          keytool -importkeystore \
            -srckeystore "$SIGNER_DIR/tenzo.p12" -srcstoretype PKCS12 -srcstorepass "$PASS" \
            -destkeystore "$KEYSTORE" -deststoretype PKCS12 -deststorepass "$PASS" \
            -alias "$KEYALIAS" -noprompt
          keytool -importcert -trustcacerts -alias "rootCA_${UNIQUE_ID}" \
            -file "$SIGNER_DIR/rootCA.pem" -keystore "$KEYSTORE" -storepass "$PASS" -noprompt
          sudo keytool -list -cacerts -storepass changeit -alias "tenzoRoot" >/dev/null 2>&1 && \
            sudo keytool -delete -cacerts -storepass changeit -alias "tenzoRoot" -noprompt || true
          sudo keytool -importcert -alias "tenzoRoot" -file "$SIGNER_DIR/rootCA.pem" \
            -cacerts -storepass changeit -noprompt && \
            echo "✔ Root CA added to Java truststore" || echo "⚠️  Failed to add Root CA"
          echo "✅ Keystore ready: $KEYSTORE"
          keytool -list -v -keystore "$KEYSTORE" -storepass "$PASS" -alias "$KEYALIAS" 2>/dev/null | grep "SHA256:" || true
          echo "SIGNER_DIR=$SIGNER_DIR" >> $GITHUB_ENV

      - name: Sign flashable ZIP
        if: ${{ github.event.inputs.enable_signing == 'yes' }}
        run: |
          set -e
          SIGNER_DIR="$HOME/.gf"
          KEYSTORE="$SIGNER_DIR/keystore.p12"
          PASS=$(cat "$SIGNER_DIR/.store_pass")
          KEYALIAS=$(cat "$SIGNER_DIR/.alias")
          SIGNED="${ZIP_FILE%.zip}-signed.zip"
          cp "$ZIP_FILE" "$SIGNED"
          SIGNING_OUTPUT=$(jarsigner \
            -keystore "$KEYSTORE" -storepass "$PASS" -keypass "$PASS" \
            -sigalg SHA256withRSA -digestalg SHA-256 \
            -tsa http://timestamp.digicert.com \
            "$SIGNED" "$KEYALIAS" 2>&1)
          echo "$SIGNING_OUTPUT"
          echo "$SIGNING_OUTPUT" | grep -q "jar signed." \
            && echo "✅ ZIP signed successfully: $SIGNED" \
            || { echo "❌ Signing failed!"; exit 1; }
          echo "ZIP_FILE=$SIGNED" >> $GITHUB_ENV

      - name: Upload flashable ZIP
        uses: actions/upload-artifact@v4
        with:
          name: SlimLLVM-${{ github.event.inputs.build_tag }}-Kernel
          path: ${{ env.ZIP_FILE }}

      - name: Push ZIP to Telegram
        if: success()
        run: |
          MD5CHECK=$(md5sum "$ZIP_FILE" | cut -d' ' -f1)
          SIZE=$(du -sh "$ZIP_FILE" | cut -f1)
          CAPTION="✅ Build Successful
          Branch: ${{ github.event.inputs.kernel_branch }}
          Tag: ${{ github.event.inputs.build_tag }}
          KernelSU Next: ${{ github.event.inputs.enable_kernelsu_next }}
          Signed: ${{ github.event.inputs.enable_signing }}
          Compiler: ${{ env.CLANG_LABEL }}
          Size: $SIZE
          MD5: $MD5CHECK"
          curl -s -F document=@"$ZIP_FILE" "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
            -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -F caption="$CAPTION"

      - name: Notify Telegram - build failure
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            -d parse_mode="html" \
            -d disable_web_page_preview=true \
            -d text="❌ <b>Build Failed</b>%0A<b>Branch:</b> <code>${{ github.event.inputs.kernel_branch }}</code>%0A<b>Tag:</b> <code>${{ github.event.inputs.build_tag }}</code>%0ACheck the Actions log for full details: %0A${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          if [ -f build.log ]; then
            curl -s -F document=@build.log "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
              -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
              -F caption="Build log for failed run (${{ github.run_id }})"
          fi